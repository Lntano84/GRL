# 审计条目状态追踪

> 日期：2026-09-18（**全部条目已关闭**）
> 审计来源：`C:\Users\windows\Documents\Codex\2026-09-18\he\outputs\GRL_后续论文方向评估_2026-09-18.md`
> 　　　　　`C:\Users\windows\Documents\Codex\2026-09-17\codex-threads-01a0adec-2761-7950-a376\outputs\GRL_下一步方向与证据审计_20260917.md`
> 复现脚本：`scripts/audit/check_audit_items.py`（逐项对照**当前源码**判定，不凭记忆）
> 完整报告：`docs/P0_COMPLETION_REPORT.md`

---

## 0. 结论（2026-09-18 收尾）

**9 项全部 RESOLVED**，每项都有一个直接探测当前代码的检查：

```
RESOLVED 9 | PARTIAL 0 | OPEN 0
RESOLVED: P0-1, P0-2a, P0-2b, P0-3a, P0-3b, P0-4, P1-1, P1-2, P1-3
```

但随着 P0-3b 的取证，发现了**两个独立的、比审计条目本身更严重的问题**：

1. **状态机在 `0248612` 之前是错的**（正激活节点永不转负、`δ=τ=1` 被拒绝）。修好后
   Congress-Twitter 的实测 spread 掉了 60–63%（k=1: 299.35 → 111.44），**比论文报告过的
   任何一个效应都大**。因此论文里所有实测数字已标注 `\withdrawn`（保留，不删除）。
2. **论文引用了 `MC = 12` 的秩相关**。产出该表的命令记录在 `docs/GATE3A_REPORT.md`，
   且同一段就写着"只适合看趋势、引用相关性数字时必须提高 mc-runs"；`notes/CLAIMS.md` C2
   也独立警告过"只用符号、不用量级"。论文摘要、引言、regime 节却引用了量级，
   违反论文自己写下的 `MC ≥ 300` 约束。**这一条与状态机 bug 无关，是独立缺陷。**

在 `MC = 300` + 修复后的模型下重测（配对逐次估计），Congress-Twitter 20% 格：
负边际占比 **44% → 4%**（超过噪声的 **0%**）、`ρ_degree` **−0.112 → +0.039**、
`ρ_δ2` **+0.034 → −0.060**。即"大量负边际 + δ₂ 与度数分离"这一主现象，
在功率足够的测量下**看不到**。八图扫描仍在跑。

---

## 1. 审计快照与当前代码的差异（历史记录）

审计的 evidence JSON 记录了四个文件的 SHA-256。与当时的对比：

| 文件 | 审计记录 | 当时 | 结论 |
| --- | --- | --- | --- |
| `src/grl/diffusion/overexposure.py` | `2d1e841c…` | `218b1243…` | **不同** |
| `scripts/experiments/evaluate_overexposure_monotonicity_rr.py` | `eb7d328d…` | `eb7d328d…` | 相同 |
| `scripts/experiments/verify_surrogate_bound.py` | `3066d540…` | `3066d540…` | 相同 |
| `src/grl/algorithms/sequential_im.py` | `b55a9c72…` | `b55a9c72…` | 相同 |

**⇒ 最新审计（9/18 11:27）是在 `overexposure.py` 修复前（提交 `0248612`，11:15）的代码上做的**，
但它引用了我的修复报告数字（报告自己也标注"本次未复跑"）。
除 `overexposure.py` 外其余三个被审计文件此后未改动。

---

## 1′. 逐项状态（全部已关闭）

| 条目 | 状态 | 当前证据 |
| --- | --- | --- |
| **P0-1** 正激活无法转负 | ✅ **已修复** | 反例 `spread=2`（应为 2）、`negative={a}`、`ever_positive={s,a,b}` |
| **P0-2a** `τ=1, δ=1` 边界 | ✅ **已修复** | `spread=2`、`negative={}`、`v` 为正 |
| **P0-2b** 三条退化路径需分开 | ✅ **已修复** | 四条具名 threshold law；实测 `E[κ]`：`simplex_tau_clamped_to_one` = 0.332（应 1/3）vs `uniform_lt_tau_one` = 0.498（应 1/2），**证明是不同过程**，不再混淆 |
| **P0-3a** 更窄的非单调论证 | ✅ **可陈述** | 精确例子：`F({a})=0.4800 > F({a,b})=0.3200`，故不存在与 S 无关的非负覆盖表示 |
| **P0-3b** 撤回旧 RR 论证与论文题目 | ✅ **已完成** | 题目已换；`inapplicability.tex` 已删除并由 `coverage.tex` 取代；8 张修复前的表全部带 `\withdrawn`；无存活的"RR 集为空"表述 |
| **P0-4** 目标集 D / 种子资格 / `≤k` | ✅ **已完成** | `src/grl/diffusion/contract.py`；缺 D 的 config 被拒；D 内种子被拒；`k+1` 被拒、`k` 通过；`TargetedObjective` 只在 D 上计数 |
| **P1-1** 不得称两份标准均匀阈值 LT | ✅ **已完成** | 实测 τ 边缘 `x²`、κ 边缘 `2x−x²` 吻合到 0.0073；源码中无任何一行把 simplex 路径说成均匀阈值 LT |
| **P1-2** 经验残差不是可靠性保证 | ✅ **已完成** | `certified` → `empirical_accept`，新增 `statistical_certificate` 与 `fallback_full_scan`；反例（`min_rounds=2` 默认值）已写入 docstring 与测试 |
| **P1-3** 预算/成本/划分/指标 8 项混淆 | ✅ **已完成** | 8 项各有代码修复与独立探针；其中第 6 项**未复现**（见 §2） |

**合计：9 已解决 / 0 部分 / 0 未做。**

---

## 2. P1-3 的 8 项：逐条结论

| # | 混淆 | 结论 |
| --- | --- | --- |
| 1 | `≤k` 下停止规则不一致 | **已复现** — `StoppingRule` 成为显式参数并逐次记录；纯预测策略拒绝它无法评估的规则 |
| 2 | 状态获取成本记为 0 | **已复现** — `state_cascades` 是 `mc_cascades` 的**分解**，因此收主成本单位就自动收了状态成本 |
| 3 | `MC=25` 贪心被称作 oracle | **已复现** — `reference_policy_name` 把预算写进名字；CLI 明说"参考点，不是上界" |
| 4 | 一张统计里混了 0/k/2k/3k | **已复现，并且改变了一个结论** — Congress-Twitter 上 between 标准差：`\|S\|=0` 为 **34.86**、`\|S\|=2` 为 4.32、`\|S\|=4` 为 2.16，池化比例被未饱和格拉低。Stage 4 的 ca_GrQc `0.64`、NetHEPT `0.075`（"非状态依赖"）可能是池化造成的 |
| 5 | 单一随机候选池 | **已复现** — 默认按度分层，并重复 `--pool-draws` 次；Congress-Twitter k=2 的度数 gap 在两个池之间为 0.90%–13.49% |
| 6 | 划分未去重 | **仅为指控，未复现** — 12/24/48/120 个 context 下重复状态数均为 0。现按规范状态分组，但那是**护栏**，没有任何数字因此改变，代码与本报告都明确这么说 |
| 7 | `label_std` 记为 NaN | **已复现** — `score_with_uncertainty` 给出配对标准误，本来零额外成本 |
| 8 | 权重归一到恰好 1（模型只要求 ≤ 1） | **已复现** — 四种具名策略；稀疏文件上 `sum_to_one` 与 `clip_to_one` 的暴露尺度差 **50×** |

---

## 3. 仍未完成

1. `MC = 300` 八图扫描未跑完（脚本可 `--resume`，每格写一次 JSON）。
2. Stage 4b 去掉两项混淆后的结论未知（重跑进行中）。
3. `contract.py` **尚未接入实验脚本**：脚本仍各自声明目标集与预算，因此目前没有任何
   重新推导出来的数字是"契约干净"的。
4. 尚未在完整契约下重新推导任何参考数字 —— 这是让 `\withdrawn` 标记摘下来的前提。
5. 论文 21 页对 16 页上限；引用仍未核实。
6. 预注册 gate 仍未冻结，需要 PI 确认阈值。

---

## 2′. 审计原文的 8 项待修（**历史记录，逐条已处理**）

以下为审计当时的原始表述，保留以便对照；每条的最终结论见 §2 的表格。

1. `full_oracle_greedy` 与 adaptive 会继续选**负边际**节点，而另一脚本遇非正增益即停；原问题 `≤k`，各方法必须**一致允许 STOP / 保留最好前缀**；
2. `delta2` 的曝光由一次级联生成，却报成"零成本"——它是**无训练、无额外候选 MC**，**不是零计算、零观测**；若真实场景免费提供观测，必须让所有方法获得同样信息；
3. `MC=25` 的 greedy **不是精确 oracle，也不是全局最优**；更便宜的训练 MC 或略高的评测值都不能解释为"超过最优"；
4. Stage 4b 用 `0,k,2k,3k` 混算 within/between，可能主要测到**随基数变化的整体增益下降**，未证明同基数下排序交叉；`k=1` 时决策前恒为空集；
5. Stage 4b 用**随机 30 节点候选池**，按该池 degree gap 选研究区间存在**选择偏差**；应预先冻结候选策略并报告全候选/高召回参照；
6. 数据按 `ctx_XXXXX` 划分，但空集、degree 前缀等可能对应**重复 S**；应按**规范化 seed set** 去重后再划分；
7. `label_std` 记为 `NaN`，**无法判断候选差是否已超过估计误差**；配对不消除差分采样噪声，需存逐样本差或其标准误；
8. 原文允许入权和 `≤1`，现有脚本把非零入权**全部归一到 1**；这是可选设定，不是论文强制条件。

---

## 3. 修复顺序（沿用审查建议）

| 优先级 | 动作 | 状态 |
| --- | --- | --- |
| **1** | **P0-3b**：撤换论文题目与 §3 论证 | ✅ 完成（`ed0a3d7e`、`7dc14d9`） |
| **2** | **P0-4**：冻结模型合同（目标集 D、`S ⊆ V\D`、`≤k`、最终正状态口径） | ✅ 完成（`src/grl/diffusion/contract.py`） |
| **3** | **P0-2b**：把三条退化路径分开暴露 | ✅ 完成（四条具名 law，数值验证互不相同） |
| **4** | **P1-2**：把 `certified` 改名为 `empirical_accept` 或补真正的覆盖保证 | ✅ 完成（三段拆分，反例入文档与测试） |
| **5** | **P1-3**：8 项成本/划分/指标口径 | ✅ 完成（8 项各有修复与探针；第 6 项未复现，作为护栏保留） |

**下一步（原顺序已走完）**：把 `contract.py` 接入实验脚本 → 在契约下重新推导全部参考数字 →
摘掉论文里的 `\withdrawn` 标记。

---

## 4. 论文层面的处置

审查明确判定：**19 页初稿是素材库,不是可通过压页完成投稿的稿件**;主论证与主实验需重建。
处置：

- 保留为历史草稿,**停止继续填入旧数字**；
- 题目与 §3（不适用性）**必须重写**为更窄的论证：
  > 对任何与 `S` 无关的随机集合 `R`，`c·Pr[S∩R≠∅]` 随 `S` 单调不减,故**标准非负覆盖型 RR 等价式**不能等于一个非单调目标；
  > 精确反例：`a→t`、`b→t` 各 0.4,`D={t}`，`F({a})=0.48 > F({a,b})=0.32`。
  > **不排除**有符号分解、其他采样结构、受限实例算法;也**不证明** MC 是唯一可用估计器。
- 建议工作题目（审查给出）：**过度暴露影响最大化中的样本高效选种与统计误差控制**
  （*Sample-Efficient Seed Selection under Overexposure with Statistical Error Control*）——
  **只有在证明学习相对最强无学习方法有独立贡献后,才把"学习增强"写进题目。**

---

## 5. 预注册门槛（待 PI 冻结）

审查建议：

> 在三张预先选定图上，**相对高精度参考的质量损失 ≤ 1% 容差**，
> 同时**相对最强无学习方法减少 ≥ 30% 的在线级联模拟**。

配套要求：报告配对差及其置信区间（非仅均值）、不同图分别报告、失败/未认证查询比例、
完整模拟次数、真实运行时间、状态获取成本、候选筛选成本、训练标签生成与训练耗时。
目标值或增量接近零时改用**目标人数单位的绝对容差**。
摊销须给出 `C_offline + Q·C_online` 的盈亏平衡点 `Q`。

**这两个数字尚未冻结**，需 PI 确认后写入实验协议。
